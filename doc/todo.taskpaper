- Set correct mime-type - try using an image for input.
- Handle 404s properly.
- Store the page in memcached for a while after scraping.
- Add (short!) timeout - searching for hello.com freezes the app.
- Handle redirects/base url - eg recurser.com.
- Add a link to the top page in the cache tag.